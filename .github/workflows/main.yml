name: Deploy

on:
  push:
    branches: [ dev ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          path: /var/www/meuapp

      # Exemplos opcionais, dependendo do seu stack:
      # - name: Instalar dependências
      #   run: cd /var/www/meuapp && npm install --production

      # - name: Reiniciar serviço
      #   run: sudo systemctl restart meuapp
